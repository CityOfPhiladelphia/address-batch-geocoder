name: Test Push or Pull Request To Main
on:
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
      - reopened
    branches:
      - main

env:
  AIS_API_KEY: ${{ secrets.AIS_API_KEY }}  

jobs:
  test:
    runs-on: self-hosted
    steps:
      - name: Get code
        uses: actions/checkout@v5
      - name: Install Git
        run: sudo apt-get install -y git
      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          version: "0.9.5"
      - name: Install project
        run: uv sync
      - name: Run tests
        run: uv run pytest tests